@model DormitoryManagement.Application.Dtos.Requests.LoginRequest
@{
    ViewData["Title"] = "Đăng nhập hệ thống";
    Layout = "_AuthLayout";
}
@section Styles {
    <style>
        /* Container căn giữa Card */
        .login-container {
            max-width: 480px;
            margin: 40px auto;
        }

        .card-login {
            border: 1px solid rgba(255, 255, 255, 0.2);
            border-radius: 20px;
            background: rgba(255, 255, 255, 0.9);
            backdrop-filter: blur(15px);
            -webkit-backdrop-filter: blur(15px);
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.25);
            overflow: hidden;
            transition: all 0.4s cubic-bezier(0.165, 0.84, 0.44, 1);
        }

        .card-login:hover {
            transform: translateY(-5px);
            box-shadow: 0 30px 50px rgba(0, 0, 0, 0.3);
        }

        .login-header {
            background: linear-gradient(135deg, rgba(0, 107, 115, 0.95) 0%, rgba(0, 77, 84, 0.95) 100%);
            color: white;
            padding: 35px 30px;
            text-align: center;
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        }

        .logo-title {
            font-size: 42px;
            font-weight: 900;
            line-height: 1;
            letter-spacing: 2px;
            text-shadow: 0 2px 4px rgba(0,0,0,0.2);
        }

        .logo-sub {
            font-weight: 700;
            letter-spacing: 2px;
            font-size: 11px;
            color: #ff9f43;
            margin-top: 5px;
        }

        /* Tabs custom glassmorphism style */
        .nav-tabs {
            border: none;
            background: rgba(0, 0, 0, 0.05);
            border-radius: 10px;
            padding: 4px;
        }

        .nav-tabs .nav-link {
            border: none;
            color: #555;
            font-weight: 600;
            border-radius: 8px;
            padding: 8px 16px;
            transition: all 0.3s ease;
        }

        .nav-tabs .nav-link.active {
            background: white;
            color: #006b73;
            box-shadow: 0 4px 15px rgba(0,0,0,0.08);
        }

        .form-label {
            color: #444;
        }

        .form-control {
            height: 48px;
            border-radius: 10px;
            border: 1px solid #dcdde1;
            padding-left: 15px;
            transition: all 0.3s;
        }

        .form-control:focus {
            border-color: #006b73;
            box-shadow: 0 0 0 0.25rem rgba(0, 107, 115, 0.15);
            background-color: #fff;
        }

        .input-group-text {
            background: #f8f9fa;
            border: 1px solid #dcdde1;
            color: #718093;
            border-radius: 10px;
            transition: all 0.3s;
        }

        .input-group:focus-within .input-group-text {
            border-color: #006b73;
            color: #006b73;
        }

        .btn-login {
            height: 50px;
            background: linear-gradient(135deg, #006b73 0%, #00545b 100%);
            color: white;
            font-weight: 700;
            border-radius: 12px;
            border: none;
            transition: all 0.3s ease;
            width: 100%;
            letter-spacing: 1px;
            box-shadow: 0 4px 15px rgba(0, 107, 115, 0.3);
        }

        .btn-login:hover {
            background: linear-gradient(135deg, #00545b 0%, #003d42 100%);
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(0, 107, 115, 0.4);
            color: white;
        }

        .btn-login:active {
            transform: translateY(0);
        }

        .btn-google {
            height: 50px;
            border-radius: 12px;
            font-weight: 600;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 10px;
            background: #fff;
            border: 1px solid #dcdde1;
            color: #2f3640;
            transition: all 0.3s ease;
            box-shadow: 0 2px 5px rgba(0,0,0,0.05);
        }

        .btn-google:hover {
            background: #f5f6fa;
            border-color: #006b73;
            box-shadow: 0 8px 20px rgba(0,0,0,0.08) !important;
            color: #000;
            transform: translateY(-2px);
        }

        .btn-google:hover i {
            transform: scale(1.15);
            transition: 0.3s;
        }
    </style>
}

<div class="login-container">
    <div class="card card-login">
        <div class="login-header">
            <div class="logo-title">DMS</div>
            <div class="logo-sub">DORMITORY MANAGEMENT</div>
            <p class="mb-0 mt-2 opacity-75 small">Vui lòng đăng nhập để tiếp tục</p>
        </div>

        <div class="card-body p-4 p-md-5">
            <!-- Tabs Lựa chọn hình thức đăng nhập -->
            <ul class="nav nav-tabs nav-fill mb-4" id="loginTab" role="tablist">
                <li class="nav-item">
                    <button class="nav-link active" id="system-tab" data-bs-toggle="tab" data-bs-target="#system-pane" type="button">Hệ thống</button>
                </li>
                <li class="nav-item">
                    <button class="nav-link" id="google-tab" data-bs-toggle="tab" data-bs-target="#google-pane" type="button">Google</button>
                </li>
            </ul>

            <div class="tab-content">
                <!-- FORM HỆ THỐNG -->
                <div class="tab-pane fade show active" id="system-pane">
                    <form asp-action="Login" method="post">
                        <div asp-validation-summary="All" class="text-danger mb-3 small"></div>

                        <div class="mb-3">
                            <label class="form-label small fw-bold">Tên đăng nhập / Email</label>
                            <div class="input-group">
                                <span class="input-group-text border-end-0 bg-light"><i class="fas fa-user"></i></span>
                                <input asp-for="Username" class="form-control border-start-0" placeholder="Nhập tài khoản" required />
                            </div>
                        </div>

                        <div class="mb-3">
                            <div class="d-flex justify-content-between">
                                <label class="form-label small fw-bold">Mật khẩu</label>
                                <a href="@Url.Action("ForgotPassword", "Account")" class="small text-decoration-none fw-bold" style="color: #006b73;">Quên?</a>
                            </div>
                            <div class="input-group">
                                <span class="input-group-text border-end-0 bg-light"><i class="fas fa-lock"></i></span>
                                <input asp-for="Password" type="password" class="form-control border-start-0 border-end-0" id="loginPwd" placeholder="********" required />
                                <span class="input-group-text" onclick="toggleLoginPass()">
                                    <i class="fas fa-eye" id="eyeIcon"></i>
                                </span>
                            </div>
                        </div>

                        <div class="form-check mb-4">
                            <input class="form-check-input" type="checkbox" id="rememberMe">
                            <label class="form-check-label small text-muted" for="rememberMe">Duy trì đăng nhập</label>
                        </div>

                        <button type="submit" class="btn btn-login shadow-sm">ĐĂNG NHẬP NGAY</button>
                    </form>
                </div>

                <!-- FORM GOOGLE -->
                <div class="tab-pane fade" id="google-pane">
                    <div class="text-center py-4">
                        <p class="text-muted small mb-4">Sử dụng tài khoản Google để truy cập nhanh vào hệ thống.</p>
                        
                        <form asp-action="ExternalLogin" asp-controller="Account" method="post">
                            <button type="submit" name="provider" value="Google" class="btn btn-google w-100 shadow-sm">
                                <!-- Sử dụng Font Awesome icon thay cho ảnh lỗi -->
                                <i class="fab fa-google me-2" style="color: #DB4437; font-size: 1.2rem;"></i>
                                Đăng nhập với Google
                            </button>
                        </form>
                        
                        <div class="mt-4 p-3 bg-light rounded-3">
                            <p class="small text-muted mb-0">
                                <i class="fas fa-info-circle me-1"></i> 
                                Khuyến khích sử dụng Email sinh viên/nhân viên (@@school.edu.vn)
                            </p>
                        </div>
                    </div>
                </div>
            </div>

            <div class="text-center mt-4 pt-3 border-top">
                <span class="text-muted small">Bạn chưa có tài khoản?</span>
                <a asp-action="Register" class="fw-bold text-decoration-none ms-1" style="color: #006b73;">Đăng ký ngay</a>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <script>
        function toggleLoginPass() {
            const input = document.getElementById('loginPwd');
            const icon = document.getElementById('eyeIcon');
            if (input.type === "password") {
                input.type = "text";
                icon.classList.replace('fa-eye', 'fa-eye-slash');
            } else {
                input.type = "password";
                icon.classList.replace('fa-eye-slash', 'fa-eye');
            }
        }
    </script>
}